# Ring Sizer (Flipper Zero) — v8 Polish + Fixes

## Changes
- Removed **Exit** menu item (use **BACK** from menu to exit).
- Added labels to measurements: **D**, **US**, **EU**.
- Circle centered correctly at (64, 32) on all circle screens.
- Updated icon to transparent paletted PNG (fixes black box on some firmwares).
- Keeps portable UI core: single fullscreen ViewPort, no ViewDispatcher/Submenu modules.

## Build / Run
```bash
ufbt
ufbt launch
```


### v9
Icon updated: white ring on transparent background (prevents black-box icon on some firmwares).
